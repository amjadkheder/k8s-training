apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deploy
  labels:
    app: web
spec:
  template:
    metadata:
      name: nginx-pod
    spec:
      containers:
        - name: nginx
          image :  nginx:alpine
  replicas: 3
  selector:
    matchLabels:
      type: frontend